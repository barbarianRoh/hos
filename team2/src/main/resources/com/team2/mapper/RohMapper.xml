<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.team2.mapper.RohMapper">

	<insert id="signup">
		insert into p2_member values(#{id},#{pw},#{name},#{birth},#{memberType},#{ip},sysdate,#{enabled},#{etc1},#{etc2})
	</insert>

	<select id="signin" resultType="int">
		select count(*) from p2_member where id=#{id} and pw=#{pw}
	</select>
	
	<delete id="withdrawal">
		delete from p2_member where id=#{id} and pw=#{pw} 
	</delete>
	
	<select id="myinfo" resultType="RohDTO">
		select * from p2_member where id=#{id}
	</select>
	
	<!-- 추후에 더 늘어날 수 있어서 동적 쿼리문으로 작성함 -->
	<update id="myinfoUpdate">
		update p2_member
		<set>
			<if test="pw != null">pw=#{pw2}</if>
		</set>
		where id=#{id} and pw=#{pw}
	</update>
	
	<select id="findMyId" resultType="RohDTO">
		select * from p2_member where name=#{name} and birth=#{birth}
	</select>
	
	<select id="findMyPw" resultType="RohDTO">
		select * from p2_member where id=#{id} and name=#{name} and birth=#{birth}
	</select>
</mapper>